{{#resume.basics}}
<header class="resume-header clearfix">
    <div class="profile-header pull-left">
        <div class="resume-title">
            <h1 class="resume-title__name">{{name}}</h1>
            <span class="resume-title__label">{{label}}</span>
        </div>
    </div>
</header>
{{/resume.basics}}
